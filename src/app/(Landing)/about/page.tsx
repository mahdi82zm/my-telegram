import AboutIndex from "@/components/pages/(Landing)/About/AboutIndex";
import { Button } from "@/components/ui/button";

export default async function page({  }) {
  return (
    <div dir="rtl">
      <AboutIndex />
    </div>
  );
}
